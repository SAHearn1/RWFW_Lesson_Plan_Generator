# Root Work Framework Lesson Plan Generator - Environment Variables
# ==================================================================
# IMPORTANT: This is a template file. DO NOT add real secrets here!
#
# Setup Instructions:
# 1. Copy this file to .env.local for local development:
#    cp .env.example .env.local
# 2. Fill in your real values in .env.local
# 3. For production, set these in your deployment platform (Vercel, etc.)
# 4. NEVER commit .env or .env.local to git!

# ==================================================================
# DATABASE
# ==================================================================
# PostgreSQL connection string from your database provider
# Examples:
# - Vercel Postgres: postgres://username:password@host:5432/database?sslmode=require
# - Supabase: postgresql://postgres:password@db.xxxxx.supabase.co:5432/postgres
# - Local: postgresql://localhost:5432/rwfw_lesson_plan_generator
DATABASE_URL="postgresql://..."

# If using Prisma Accelerate, uncomment and set direct database connection:
# DIRECT_DATABASE_URL="postgresql://..."

# ==================================================================
# AUTHENTICATION (NextAuth.js)
# ==================================================================
# Generate a random secret with: openssl rand -base64 32
# This is used to encrypt session tokens and should be unique and secret
NEXTAUTH_SECRET="your-secret-here-generate-with-openssl-rand-base64-32"

# The base URL of your application
# Local development: http://localhost:3000
# Production: https://your-domain.com
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth Credentials
# Get these from: https://console.cloud.google.com/apis/credentials
# 1. Create OAuth 2.0 Client ID
# 2. Add authorized redirect URI: http://localhost:3000/api/auth/callback/google (local)
# 3. Add authorized redirect URI: https://your-domain.com/api/auth/callback/google (production)
GOOGLE_CLIENT_ID="your-google-client-id.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ==================================================================
# AI PROVIDER (Anthropic Claude)
# ==================================================================
# Get your API key from: https://console.anthropic.com/
# This is used to generate lesson plans using Claude AI
ANTHROPIC_API_KEY="sk-ant-..."

# ==================================================================
# PAYMENT PROVIDER (Stripe)
# ==================================================================
# Get these from: https://dashboard.stripe.com/apikeys
# Use test keys (sk_test_...) for development
# Use live keys (sk_live_...) for production
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# ==================================================================
# OPTIONAL: Additional Configuration
# ==================================================================
# Uncomment and configure as needed:

# Node environment (development, production, test)
# NODE_ENV="development"

# Logging level (debug, info, warn, error)
# LOG_LEVEL="info"
